Rewrite the user's latest message as a standalone search query using the conversation summary and recent turns.
Resolve pronouns and references ("it", "that", "the second option") to the concrete entities they refer to.
Do not answer the question. Output only the rewritten query on one line.
